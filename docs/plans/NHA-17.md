# Implementation Plan: Telegram bot rerun not work

## Spec
The Telegram bot rerun command uses a lowercase ID and wrong state, causing git fetch to fail with exit code 128. The spec is to fix the rerun logic to use the correct case-sensitive ID and pass the current valid state. Additionally, ensure that the git fetch command runs in the correct directory with proper origin URL.

## Acceptance Criteria
- Rerun from Telegram uses the correct case-sensitive pipeline ID.
- Rerun uses the most recent valid plan state instead of a stale or incorrect state.
- Git fetch succeeds (exit 0) when rerun is triggered.
- No changes to other pipeline operations.

## Files to Change
- src/telegram_bot.py: modify
- src/pipeline_runner.py: modify

## Risk
medium

---
Generated by AI Dev Station (studio)
