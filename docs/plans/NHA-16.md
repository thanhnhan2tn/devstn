# Implementation Plan: Bug that when change tag, worker run multiple

## Spec
Fix worker triggering logic to only execute on ticket tags 'plan' or 'implement'. Prevent multiple triggers on a single tag change event and suppress triggering on ticket creation when no tag is present.

## Acceptance Criteria
- Worker does not trigger when a ticket is created without a tag.
- Worker triggers exactly once when a ticket's tag changes to 'plan' or 'implement'.
- Worker does not trigger when a ticket's tag changes to any value other than 'plan' or 'implement' (e.g., empty, 'done', 'review').
- Worker does not trigger multiple times on a single tag change event (e.g., duplicate events are ignored or deduplicated).

## Files to Change
- linear-pipeline/event_handler.py: modify
- linear-pipeline/tag_change_handler.py: modify

## Risk
medium

---
Generated by AI Dev Station (studio)
